apiVersion: v1
kind: Service
metadata:
  name: dnsweaver
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: dnsweaver
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
